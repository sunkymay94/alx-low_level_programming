#include "main.h"
/**
 * _isalpha - A function to check if e is a letter, lowercase or uppercase
 * @e: is the int that will be used for the argument of the function
 * Return: Always Succes
 */
int _isalpha(int e)
{
	if ((e >= 'a' && e <= 'z') || (e >= 'A' && c <= 'Z'))
	{
	return (1);
	}
else
	return (0);
}
