#include <stdio.h>
/**
 * main - print if the number is postive, zero, or negative
 *
 * Description: using the main function
 * Return: 0
 */
int main(void)
{
char c;
for (c = '0'; c <= 'f'; c++)
{
putchar(c);
}
putchar('\n');
return (0);
}
